# 🚀 Git Push로 Vercel 자동 배포 설정하기

## ✨ 자동 배포란?

GitHub에 `git push`만 하면 Vercel이 자동으로 감지하여 배포합니다!

```
코드 수정 → git push → Vercel 자동 배포 ✅
```

## 📋 설정 방법 (3단계)

### 1️⃣ GitHub 저장소 만들기

1. https://github.com 접속 및 로그인
2. 우측 상단 **+** 버튼 → **New repository** 클릭
3. 저장소 이름 입력: `srt-translator` (원하는 이름)
4. **Public** 또는 **Private** 선택
5. **Create repository** 클릭
6. 생성된 URL 복사 (예: `https://github.com/username/srt-translator.git`)

### 2️⃣ Git에 코드 푸시하기

#### 방법 A: 배치 파일 사용 (추천! 🌟)

1. **`git-setup.bat`** 파일을 더블클릭
2. 커밋 메시지 입력 (엔터: 기본 메시지 사용)
3. GitHub 저장소 URL 입력 (1단계에서 복사한 URL)
4. 완료! ✅

#### 방법 B: 수동으로 명령어 입력

```bash
# 1. Git 초기화
git init

# 2. 파일 추가
git add .

# 3. 커밋
git commit -m "Add SRT translator with animations"

# 4. Remote 추가
git remote add origin https://github.com/username/srt-translator.git

# 5. 브랜치 이름 변경
git branch -M main

# 6. 푸시
git push -u origin main
```

### 3️⃣ Vercel과 GitHub 연결하기

1. https://vercel.com/dashboard 접속
2. **"New Project"** 클릭
3. **"Import Git Repository"** 섹션에서 GitHub 저장소 선택
4. 저장소를 찾지 못하면 **"Adjust GitHub App Permissions"** 클릭
   - Vercel이 저장소에 접근할 수 있도록 권한 부여
5. 저장소 선택 후 **"Import"** 클릭
6. 프로젝트 설정:
   - **Framework Preset**: Other
   - **Root Directory**: ./
   - **Build Command**: (비워두기)
   - **Output Directory**: (비워두기)
7. **"Deploy"** 클릭
8. 배포 완료! 🎉

## ✅ 완료! 이제 자동 배포가 설정되었습니다!

이제부터는:

```bash
# 1. 코드 수정
# 2. Git에 커밋 & 푸시
git add .
git commit -m "Update feature"
git push

# 3. Vercel이 자동으로 감지하여 배포! 🚀
```

## 🔄 이후 수정할 때

### 매번 수정 후:

```bash
git add .
git commit -m "수정 내용 설명"
git push
```

또는 간단한 배치 파일을 만들 수도 있습니다:

**`quick-push.bat`** (아래 내용으로 생성)
```bat
@echo off
git add .
set /p msg="커밋 메시지: "
git commit -m "%msg%"
git push
echo 푸시 완료! Vercel에서 자동 배포 중...
pause
```

## 📊 Vercel에서 배포 확인하기

1. https://vercel.com/dashboard 접속
2. 프로젝트 클릭
3. **Deployments** 탭에서 배포 상태 확인
   - ⏳ Building: 배포 중
   - ✅ Ready: 배포 완료
   - ❌ Failed: 실패 (로그 확인)

## 🌿 브랜치별 배포

Vercel은 브랜치별로 자동 배포합니다:

- **main/master 브랜치** → 프로덕션 배포 (실제 URL)
- **다른 브랜치** → 프리뷰 배포 (테스트 URL)

```bash
# 개발 브랜치 생성
git checkout -b develop

# 수정 후 푸시
git add .
git commit -m "Test new feature"
git push origin develop

# → Vercel이 자동으로 프리뷰 URL 생성! 🎯
```

## 🎯 자동 배포의 장점

1. ✅ **즉시 반영**: 코드 푸시하면 바로 배포
2. ✅ **롤백 쉬움**: 이전 배포로 한 클릭으로 되돌리기
3. ✅ **프리뷰**: PR마다 별도 URL로 미리보기
4. ✅ **자동화**: 수동 배포 필요 없음
5. ✅ **무료**: Vercel 무료 플랜으로 충분

## 🔧 문제 해결

### Git push가 안 될 때

#### 1. 인증 오류
```
remote: Support for password authentication was removed
```

**해결책**: Personal Access Token 사용
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. repo 권한 체크
4. 토큰 복사
5. Git 명령어에서 비밀번호 대신 토큰 입력

#### 2. 더 쉬운 방법: GitHub Desktop 사용

1. https://desktop.github.com 에서 다운로드
2. GitHub 계정으로 로그인
3. GUI로 쉽게 커밋 & 푸시

### Vercel 자동 배포가 안 될 때

1. Vercel 대시보드에서 프로젝트 → Settings → Git
2. GitHub 연결 상태 확인
3. Webhook 재설정

## 💡 팁

### 1. .gitignore 파일
이미 준비되어 있습니다! 불필요한 파일은 자동으로 제외됩니다.

### 2. 커밋 메시지 컨벤션
```bash
git commit -m "feat: 새로운 기능 추가"
git commit -m "fix: 버그 수정"
git commit -m "style: 디자인 변경"
git commit -m "docs: 문서 수정"
```

### 3. 배포 알림
Vercel Dashboard → Project Settings → Notifications에서
이메일 또는 Slack 알림 설정 가능

## 🎊 완료!

이제 편하게 코드를 수정하고 `git push`만 하면
Vercel이 자동으로 배포합니다! 🚀

**다음 단계:**
1. `git-setup.bat` 실행하여 GitHub에 푸시
2. Vercel과 GitHub 저장소 연결
3. 이후부터는 `git push`만 하면 끝!

